import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions'

import References from './index'

<Meta title="core|Components/References" component={References}/>

export const styles = {
  container: {
    width: '100%',
    maxWidth: '600px'
  }
}

# References

Demonstrates default rendering of references

<Preview>
  <Story name="default">
    <div style={styles.container}>
      <p>Before</p>
      <References
          references={[
            { id: 'id1', title: 'title1' },
            { id: 'id2', title: 'title2' },
            { id: 'id3', title: 'title3' },
          ]}
        />
        <p>After</p>
    </div>
  </Story>
</Preview>

Demonstrates rendering of references with a different label

<Preview>
  <Story name="label">
    <div style={styles.container}>
        <p>Before</p>
        <References
          referencesLabel="Works Cited"
          references={[
            { id: 'id1', title: 'title1' },
            { id: 'id2', title: 'title2' },
            { id: 'id3', title: 'title3' },
          ]}
        />
        <p>After</p>
    </div>
  </Story>
</Preview>