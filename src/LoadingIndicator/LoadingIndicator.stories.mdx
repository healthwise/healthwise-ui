import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { ArgsTable } from '@storybook/addon-docs'

import LoadingIndicator from './index'

<Meta title="UI Feedback/Loading Indicator" component={LoadingIndicator} />

export const styles = {
  invertedContainer: {
    background: '#424242',
  },
}

# Loading Indicator

Default rendering.

<Preview>
  <Story name="default">
    <LoadingIndicator />
  </Story>
</Preview>

With hidden text.

<Preview>
  <Story name="with hidden text">
    <LoadingIndicator hiddenText="This is the custom hidden text" />
    Use a screen reader to hear custom hidden text
  </Story>
</Preview>

With inverted color.

<Preview>
  <Story name="inverted">
    <div style={styles.invertedContainer}>
      <LoadingIndicator inverted />
    </div>
  </Story>
</Preview>

## Props

<ArgsTable of={LoadingIndicator} />
