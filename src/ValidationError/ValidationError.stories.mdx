import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions'

import ValidationError from './index'

export const styles = {
  input: {
    marginRight: '16px',
    padding: '8px',
    width: '200px',
  },
}
/* Hack to make ValidationError show */
const head = document.head
const style = document.createElement('style')
const css = '.validation-error-container label { display: inline-block }'
style.appendChild(document.createTextNode(css))
head.appendChild(style)

<Meta title="core|UI Feedback/ValidationError" component={ValidationError} />

# ValidationError
Demonstrates default rendering of ValidationError component.

<Preview>
  <Story name="default">
    <div className="validation-error-container">
      <input type="email" id="test-input" style={styles.input} placeholder="Text input" />
      <ValidationError forId="test-input" style={{ display: 'inline-block' }}>
        ValidationError message
      </ValidationError>
    </div>
  </Story>
</Preview>